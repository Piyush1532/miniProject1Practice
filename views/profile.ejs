<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="w-full min-h-screen bg-zinc-900 text-white p-10"
>
<div class="flex w-full justify-end ">
<a href="/logout" class="bg-red-700 rounded-md px-3 py-2 text-sm inline-block ">Logout</a>
</div>
<div class="flex item-start gap-3">
    <div class="w-10 rounded-md overflow-hidden">
        <img class="w-full h-full object-cover"  src="/images/uploads/<%= user.profilepic %>" alt="">
    </div>
    <h3 class="text-3xl mb-5">Hello <%= user.name %> 👋</h3>

</div>

<h5 class="mb-5">You can create a new post</h5>
<form action="/post" method="post">
    <textarea placeholder="What's on your mind" name="content" class="w-1/3 outline-none resize-none bg-transparent border-2 border-zinc-700 rounded-md p-3 block " ></textarea>
    <input type="submit" value="Create new post" class="bg-blue-500 rounded-md outline-none px-3 py-2 block mt-4 w-40">
</form>
<div class="posts mt-10">
<p class="text-zinc-400">New posts</p>

<div class="post-container mt-5">
<% user.posts.reverse().forEach(function(post){ %>

    <div class="post w-1/3 p-4 border-[1px] border-gray-600 rounded-md bg-zinc-800 mt-2 ">
        <h4 class="text-blue-500 mb-2"> @<%= user.username%> </h4>
        <p class="text-sm tracking-tight">
            <%= post.content%>
        </p>
        <small class="inline-block mt-2"><%= post.likes.length %> likes</small>
        <div class="btns flex mt-1 gap-4">
            <a href="/like/<%=post._id %>" class="text-blue-500">
           <%= post.likes.indexOf(user._id) === -1 ? "Like" :"Unlike" %>

            </a>
            <a href="/edit/<%= post._id%>" class="text-zinc-500">Edit</a>
        </div>
        </div>


<% }) %>





</div>





</div>

</div>
</body>
</html>