<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background-color: #1a1a1a;
      color: white;
      font-family: 'Arial', sans-serif;
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen">
  <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">
    <h1 class="text-3xl font-bold text-center text-white mb-6">Edit Post</h1>

    <form class="space-y-6" action="/update/<%= post._id%>" method="post">
      <!-- Text Area Field -->
      <div>
        <label for="post" class="block text-sm font-medium text-gray-300">Edit your post</label>
        <textarea id="post" name="content" rows="6" required 
          class="w-full mt-2 p-3 rounded-lg bg-gray-700 text-white placeholder-gray-400 focus:ring-2 focus:ring-yellow-500 focus:outline-none border border-gray-600"><%= post.content%></textarea>
      </div>

      <!-- Update Button -->
      <input type="submit" class="w-full py-3 bg-yellow-500 text-gray-900 rounded-lg font-semibold hover:bg-yellow-600 focus:ring-2 focus:ring-yellow-400 focus:outline-none transition duration-300" value="Update post"/>
    </form>
  </div>
</body>
</html>
